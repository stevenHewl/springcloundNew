
spring:
  application:
    name: eureka-provider

server:
  port: 8001

eureka:
  # 配置开启健康检查
  client:
    healthcheck:
      enabled: true
    service-url:
      # 向每个注册中心注册
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/